# =============================================================================
# Dependency versions audited 2026-06-28 (Issue #11 / #12 — version audit).
# Every pin below was installed together into a clean venv and verified
# against the full test suite + a live app boot before landing. See
# docs/deployment/DEPENDENCY_AUDIT.md for the verification notes and the
# rationale for each bump.
# =============================================================================

# -----------------------------
# Core Backend
# -----------------------------
fastapi==0.118.0
uvicorn==0.34.0

# -----------------------------
# Database & ORM
# -----------------------------
sqlalchemy==2.0.36
psycopg2-binary==2.9.10

# -----------------------------
# Configuration & Environment
# -----------------------------
pydantic==2.10.6
pydantic-settings==2.7.1
python-dotenv==1.0.1
email-validator==2.2.0

# -----------------------------
# Authentication & Security
# -----------------------------
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1

# -----------------------------
# AI / NLP
# -----------------------------
# openai 2.x retains full backward compatibility with the
# client.chat.completions.create(...) interface used in
# app/services/response_generator.py and app/services/classifier.py —
# verified directly against this codebase before bumping from 1.3.0.
openai==2.44.0
#scikit-learn==1.3.2
#numpy==1.24.3

# Optional (future upgrades)
#spacy==3.7.2
#sentence-transformers==2.2.2

# -----------------------------
# HTTP Client / Service Dependencies
# -----------------------------
httpx==0.28.1
redis==5.2.1
resend==2.5.1

# -----------------------------
# Utilities
# -----------------------------
typing-extensions==4.15.0

# -----------------------------
# Testing (optional but recommended)
# -----------------------------
pytest==8.3.4
pytest-asyncio==0.25.2
slowapi==0.1.9

alembic==1.14.0
